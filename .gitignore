# .NET
bin/
obj/
*.user
*.suo
.vs/

# Node (for Milkdown build)
node_modules/
package-lock.json

# Secrets
.env
appsettings.*.json
!appsettings.json
!appsettings.Development.json

# IDE
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Certs (generated locally)
certs/

# Reload sentinel file used by scripts/run.sh
.reload

# Data (never in repo)
noto-data/

# Origin data (symlink to chatgpt export, contains secrets)
origin/

# Generated audio demos
src/Noto.Server/wwwroot/audio/*.aiff

# EF migrations designer snapshot (auto-generated, can be large)
# Keep migrations themselves

# Tailscale auth key for deploy/provision.sh
deploy/.ts-authkey

# Ansible secrets for the noto deployment
deploy/ansible/secrets.yml

# Symlink to a local ChatGPT export used for one-off analysis. The target holds
# a live API key and ~35MB of conversation JSON; neither belongs in this repo.
/origin

# Local operator material for the deployment: WireGuard peer configs (private
# keys), the phone QR, the server root password, the Cloudflare token. Kept in
# the repo for convenience, never tracked.
/deploy/.local/
