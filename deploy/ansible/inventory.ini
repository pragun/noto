# The Linode is reachable only over WireGuard — its public interface drops
# everything except UDP 51820. Bring the tunnel up before running a play.
[noto]
noto-server ansible_host=10.77.0.1 ansible_user=root
