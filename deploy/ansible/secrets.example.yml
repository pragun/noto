---
# Copy to secrets.yml (gitignored) and fill in, or encrypt with ansible-vault:
#   ansible-vault encrypt secrets.yml
#
# cf_dns_api_token  Cloudflare token scoped to Zone:DNS:Edit on this zone only.
#                   Traefik uses it for the ACME DNS-01 challenge.
# openrouter_key    Chat/LLM features.
# openai_key        Whisper transcription. Leave blank to disable.
cf_dns_api_token: ""
openrouter_key: ""
openai_key: ""
